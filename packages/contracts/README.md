# Contracts Workspace

This package is reserved for shared contracts between:
- `apps/desktop`
- `apps/api`
- future automation workers or webhook processors

Current contents:
- Hono route factory and exported `AppType`
- RPC client factory for desktop consumers
- shared DTOs for workspace and intake preview endpoints
